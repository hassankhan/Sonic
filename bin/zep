#!/usr/bin/php
<?php

$cwd = getcwd();

if ($argc === 1 || in_array($argv[1], array('--help', '-help', '-h', '-?')) || empty($argv[1])) {
?>

This is zep, a stupidly simple way to get
you up and running with Zepto.

  Usage:
  zep init          Initial setup of Zepto app, creates
                    like, folders and things
  zep new <option>  <option> is either plugin or content

  With the --help, -help, -h, or -? options, you
  can get this help.

<?php

    // zep init
    // First ask if they want to continue. This will create
    // all required folders
    //
    // If they agree, then create 'content', 'plugins',
    // 'templates' folders.

    // zep new template (?)

    // zep new plugin
    // Ask for plugin name
    // Create plugin in folder

    // zep new content
    // Ask for title, desc, author, bla bla
    // Generate .md file in content folder

} else {
    if ($argv[1] === 'init') {
        // Check if folders already exist
        // Otherwise create them


?>
  Init innit

<?php
    } elseif ($argv[1] === 'new' && $argc === 3) {
      if ($argv[2] === 'plugin') {
        echo getcwd();
?>
  New plugin
<?php
        // Create plugin
      }
      elseif ($argv[2] === 'content') {
        // Create content
      }
    }
}
?>
